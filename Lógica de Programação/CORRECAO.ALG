Algoritmo "correcao"

Var
base, altura, area: real

Inicio
// ENTRADA
// receber base e altura

escreval("Qual é a base do retângulo?")
leia(base)
limpaTela()

escreval ("Qual é a altura do retângulo? (m2)")
leia(altura)
limpaTela()

//PROCESSAMENTO
// calcular a área

area <- base * altura
limpaTela()

//SAÍDA
// retornar a área calculada
 escreval("A área em, m2 é de :", area)
 
Fimalgoritmo